import setuptools

setuptools.setup(
    name="gcp-pde-python-mapreduce-dataflow",
    version="1.0",
    description="MapReduce Beam pipeline dependencies",
    packages=setuptools.find_packages(),
)
